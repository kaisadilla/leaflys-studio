import type { Step } from "react-joyride";

const MAP_EDITOR_JOYRIDE_STEPS: Step[] = [
  {
    target: '#document-name',
    content: "This is the document's name!",
  },
  {
    target: '#document-name',
    content: "This is the document's name!",
  },
]

export default MAP_EDITOR_JOYRIDE_STEPS;
